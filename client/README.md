# Ethara AI Client

React + Vite frontend for the Ethara AI Task app.

## Scripts

```bash
npm run dev
npm run build
npm run lint
npm run preview
```

Copy `.env.example` to `.env` and set `VITE_API_URL` if your API is not running at `http://localhost:5000/api`.
